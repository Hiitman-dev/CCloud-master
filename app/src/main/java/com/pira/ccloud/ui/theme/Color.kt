package com.pira.ccloud.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The app's default brand seed — a soft, desaturated blue used as the
 * primary accent when no custom color is chosen by the user.
 *
 * All other color roles are derived algorithmically in [ColorSchemeBuilder]
 * from this seed (or a user-picked replacement) so the whole palette stays
 * internally consistent regardless of which accent is active.
 */
val DefaultBrandSeed = Color(0xFF6E8CA8)
