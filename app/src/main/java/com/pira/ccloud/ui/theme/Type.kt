package com.pira.ccloud.ui.theme

// The active Material3 Typography is built by `appTypography()` in Theme.kt
// (it needs to react to the user's selected font family). This file is kept
// only so existing imports of `com.pira.ccloud.ui.theme` don't break; the
// design system's actual type scale lives in Theme.kt.
